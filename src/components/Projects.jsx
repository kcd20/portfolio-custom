import { Box } from "@mui/material";
import React from "react";

const classes = {
  root: {
    height: "100vh",
  },
};
const Projects = () => {
  return <Box sx={classes.root}>Projects</Box>;
};

export default Projects;
