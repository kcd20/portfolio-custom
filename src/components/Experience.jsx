import { Box } from "@mui/material";
import React from "react";

const classes = {
  root: {
    height: "100vh",
  },
};
const Experience = () => {
  return <Box sx={classes.root}>Experience</Box>;
};

export default Experience;
