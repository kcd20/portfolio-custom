import Box from "@mui/material/Box";
import React from "react";

const classes = {
  root: {
    height: "100vh",
  },
};

const Contact = () => {
  return <Box sx={classes.root}>Contact</Box>;
};

export default Contact;
