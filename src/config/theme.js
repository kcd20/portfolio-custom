import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  spacing: 8,
  palette: {
    navy: "rgb(16, 44, 87)",
    gold: "rgb(218, 192, 163)",
    beige: "rgb(234, 219, 200)",
    light: "rgb(248, 240, 229)",
  },
});

export default theme;
